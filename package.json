{
  "name": "my-sst-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.1.2",
    "@serverless-stack/resources": "^1.1.2",
    "@tsconfig/node16": "^1.0.2",
    "@types/uuid": "^8.3.4",
    "typescript": "^4.6.4",
    "vitest": "^0.12.9"
  },
  "workspaces": [
    "backend"
  ],
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
